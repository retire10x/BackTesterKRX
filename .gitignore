# 가상환경 (프로젝트 루트 기준)
venv/
.venv/

# 파이썬
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.eggs/
*.egg-info/
dist/
build/

# 도구 / IDE
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/

# 로컬·비밀
.env
.env.*
!.env.example

# 백테스트·스캔 실행 산출물 (재생성 가능, 커밋 제외)
output/
outputs/
!outputs/v5_final_report.md

# pykrx 벌크 OHLCV 로컬 캐시(재생성 가능, .gitkeep 만 추적)
data/cache/**
!data/cache/**/
!data/cache/**/.gitkeep

# GUI 마지막 세션(로컬)
config/last_session.json

# v5.3 릴레이 유니버스 — 7구간·재스캔으로 재현 (커밋 X, settings.yaml 스펙이 SSOT)
config/relay_universes/*
!config/relay_universes/.gitkeep
!config/relay_universes/README.md

# v5.1/v5_2 고정 유니버스 — lock_date 1회 박제본은 Git 추적 O (의도적 갱신 시 커밋)
# → config/kosdaq_sniper_universe.json / .meta.json (run_v5_breakout.py --scan-universe)

# v5.5.2 라이브 — 당일·개인 계좌 상태 (커밋 X)
config/live_today_universe.json
config/live_today_universe.meta.json
config/live_positions.json

# v6.0 라이브 SQLite 장부 (개인 계좌·매매 기록)
data/live_trading.db
data/live_trading.db-wal
data/live_trading.db-shm

backtest_signal_debug.txt

