# BackTesterKRX 작업지시서 (국장 퀀트 백테스터)

이 글은 **무엇을 만들지**, **어떤 순서로 만들지**, **숫자는 어떻게 낼지**를 적어 둔 공식 기준이다.

---

## 1. 프로젝트가 하는 일

- **목적:** 실전 매매 연동 **없음**. 과거 데이터만으로 가설을 검증하는 **순수 백테스트** 프로그램.
- **핵심 가치:** 봉 주기, 이평선 **N**, 종목 선택을 바꿀 때마다 **성적표 + 정적 그래프**가 갱신되어, 아이디어를 빠르게 확인할 수 있게 한다.

---

## 2. 이 프로그램이 하지 않는 일

- 실시간 자동 매매.
- 증권사 API 주문 연동.

---

## 3. 매매 규칙 (신호 정의 + 체결)

**용어:** “이평선” = 사용자가 정한 **N기간** 이동평균(종가 기준).

### v4.6 기본 스위치·필터 결합 (`golden_buy_enabled`, `dead_cross_sell_enabled`)

- **골든 끔:** 전일 종가 ≤ 이평·당일 종가 > 이평이어도 **매수 신호 행 자체 없음**(전략 `add_signals`; 시뮬은 pending 매수 신호 불가).
- **데크 끔:** 데드크로스 **신호 매도**(다음 시가 체결)는 실행 안 함 · **가변 낙폭**(켜져 있으면)만으로 청산 가능.
- **매수:** 골든 후보 발생 시 활성 진입 필터(대세·돌파·시간 버퍼) 검증 결과를 **AND** 로 결합, 모두 통과해야 **▲**(다음 시가 매수 체결).
- **매도:** 가변 낙폭(우선 순위)·데크 신호 매도 중 **먼저** 만족한 쪽(다음 시가)·**OR**(시뮬 `simulate_single`; 데크 꺼짐 시 후자만 무).

### 진입 (골든크로스)

- 전일 종가 ≤ 이평선, 당일 종가 > 이평선 → 그날 **종가**에서 신호 확정.

### 청산 (데드크로스)

- 전일 종가 ≥ 이평선, 당일 종가 < 이평선 → 그날 **종가**에서 신호 확정.

### 선택: v4.4 수익률 구간별 가변 낙폭 매도 (고점 대비 트레일)

- **설정:** GUI 우측 「매매 규칙」·`config/settings.yaml` 의 `trailing_stop_enabled`, `trailing_reference_pct`, `trailing_drop_below_pct`, `trailing_drop_above_pct`.
- **실행:** 매수 체결가 대비 보유 기간 **장중 최고가**를 워터마크로 두고, 해당 고가 기준 **피크 수익률**이 기준% **미만**이면 고가 대비 미달% 하락 종가 확정 시(다음 봉 **시가** 청산), 한 번이라도 기준% **이상**이면 고가 대비 돌파% 하락 시 동일하게 청산. 데드크로스 신호보다 **먼저** 적용될 수 있음(`src/simulator.py`).
- **차트:** 해당 매도 타점은 **`reason: trail_stop`** 으로 구분되어 밝은 노란색 ▼ 로 표시(`src/backtest_chart.py`).

### 체결

- 신호는 **그 봉 종가**에서 확정.
- 가상 체결은 **다음 봉 시가**다. (일봉 → 다음 거래일 시가, 주봉 → **다음 주 봉의 시가**.)

### 비용

- 매수 수수료 **0.015%** (0.00015).
- 매도 수수료+세금 **0.20%** (0.0020).

---

## 4. 단계별 로드맵 (Milestone)

**규칙:** 한 단계가 **완전히 돌아가고**, 유저 확인 후 다음 단계로 간다.

### 개발 집중 방향 (데이터 현실)

- **국내 주식 무료 데이터 한계**를 감안해, 당장은 **[1단계] 일봉**과 **[3단계] 주봉** 위주로 진행한다.
- **[2단계] 시간봉**은 무료로 맞추기 어려우면, **가상화폐(예: 빗썸·업비트 API)** 로 자산을 바꿀지 **유저와 먼저 상의**한 뒤 진행한다.

### [1단계] 기본 프레임 + 일봉(Daily) — 싱글 종목

**목표:** 일봉 1종목, 500만 원 **전액** 투입·전량 매매, 성과표 + 그래프까지 **실행 가능**.

**해야 할 것:**

- FinanceDataReader 로 **국내 시장** 가격 데이터.
- **종목 고르는 방법:** 시장 리스트에서 **키워드로 후보를 좁힌 뒤**, 그중 **코드 1개만** 선택해 백테스트 (`settings.yaml` + `python main.py --list`).
- **Warm-up:** 이평 왜곡 줄이기 위해 시작일 **이전** 데이터 확보.
- 터미널 **표** 출력.
- **1단계 시각화 마감:** `output/backtest_report.png` **정적 이미지** 한 장. (Plotly 등 인터랙티브 차트는 **엔진 완성 뒤** 후속.)

### [2단계] 시간봉 확장 + 이평 N 자유 입력 (보류·협의)

- 진행 전: **무료 데이터 전략**을 유저와 합의(필요 시 크립토 등).
- 합의 후: 시간봉 수집, N 변수화, “이번 봉 종가 확정 → 다음 봉 시가 체결” 유지.

### [3단계] 주봉 + 설정/CLI 강화

**목표:** 일봉 → 주봉 리샘플 후 주간 이평 시뮬, 입력은 YAML/CLI 로 **코드를 안 열어도** 되게.

**구현 요약 (현재):**

- `strategy.interval`: `daily`(일봉) 또는 `weekly`(주봉). 주봉은 일봉을 **금요일 말(`W-FRI`)** 기준으로 묶어 집계한다.
- 같은 전략·체결 규칙을 주봉에 그대로 적용한다.
- **CAGR:** 일봉일 때 연환산 분모 **252**, 주봉일 때 **52**(주 수 기준 근사).
- **CLI 덮어쓰기:**  
  `--interval daily|weekly`, `--start`, `--end`, `--keyword`, `--market`, `--code`, `--ma`

### [4단계] 과거 유니버스 추적 (생존 편향 최종 제거)

- 과거 시점의 실제 리스트·상폐 종목 반영 등. (별도 지시 후 진행.)

---

## 5. 성적표 지표

**표시:** 퍼센트(%)는 **소수 둘째 자리**까지 (예: `12.34%`).

포함 항목:

1. **누적 수익률(%)**
2. **CAGR (%):** `(최종자산 / 최초자산) ^ (1 / (전체거래일수 / 252)) - 1` 를 퍼센트로 표시.  
   - **구현:** 일봉은 분모 **252**, 주봉은 **52**(봉 개수로 나눈 “해 수”에 맞춤).
3. **MDD (최대 낙폭, %):** 지식 베이스 정의는  
   `Drawdown = (누적최대수익률 - 현재수익률) / 누적최대수익률`, `MDD = max(Drawdown)` 이다.

> **구현 메모 (1단계):** 위 MDD 분수 정의는 **누적 최고 수익률이 0에 매우 가까울 때** 값이 비정상적으로 커질 수 있다. 그래서 **1단계 코드**에서는 성적표의 MDD를 **평가액(자산) 최고점 대비 낙폭** `(피크 자산 - 현재 자산) / 피크 자산` 의 최댓값으로 표시한다. (신뢰 가능한 숫자 우선.)

### 5.1 사용자 화면 표기 (GUI·CLI, v2.4)

내부 구현·주석·수식 설명에서는 **CAGR**, **MDD** 등 영문 약어를 써도 된다. 다만 **`summary_rows` 로 GUI(`CTkTextbox`)·터미널 표에 넣는 첫 번째 열(항목명)** 은 유저 친화적으로 다음처럼 **한글 고정**으로 맞춘다.

| 내부 지표 | 표에 쓰는 이름 |
|-----------|----------------|
| CAGR      | **연평균 수익률** |
| MDD       | **최대 손실 낙폭** |

예: `CAGR: -4.03%` 가 아니라 `연평균 수익률: -4.03%`, `MDD: 55.26%` 가 아니라 `최대 손실 낙폭: 55.26%`.

---

## 6. 개발자에게 전하는 말

이 프로젝트는 **자동 매매가 아니다.** 가설을 과거에 대입해 **성적과 그래프**를 내는 **분석용**이다.

**[1단계]**·**[3단계]**·**§8 시각화**·**§10 GUI(v2.4)** 까지가 현재 구현 범위다. **[4단계] 과거 유니버스·생존 편향 제거**는 **GUI 안정화 이후** 순차 진행한다.

---

## 7. 클라이언트(유저) 추가 확정 사항 (개발자 필독)

1. **유니버스:** 전종목 분산 투자 **아님**. 키워드 등으로 **후보 리스트**를 만든 뒤, 그중 **선택한 종목 1개만** 매수·매도한다. 초기 **500만 원 전액** 투입.
2. **데이터·단계:** 무료 한계를 감안해 **1단계(일봉)·3단계(주봉)** 에 집중. **2단계(시간봉)** 는 무료 구현 방안으로 **가상화폐 API 등 전환** 여부를 **유저와 사전 상의**.
3. **표기:** 성적표의 **모든 % 지표**는 **소수 둘째 자리** (예: `12.34%`).
4. **차트:** 산출물은 `output/backtest_report.png` **정적 이미지**. 인터랙티브(Plotly 등)는 **후순위**. 다만 **§8** 처럼 정적 PNG 안에서도 **매매 타점 표기**는 한다.

---

## 8. 시각화 — 캔들·거래량·매매 타점 (v2.4 ~ v2.6)

`output/backtest_report.png` 는 **`mplfinance`** 로 렌더한다. (선 차트 **종가 단일 곡선**은 사용하지 않음.)

- **한글 폰트(v2.6):** `make_mpf_style(..., rc={...})` 에 설치된 **맑은 고딕** 등 **sans-serif** 를 넣고, `matplotlib.pyplot.rcParams` 에도 동기화해 **타이틀·축 라벨 깨짐(□)** 을 방지한다.

- **1패널(주가):** **한국식 OHLC 캔들** — 양봉 빨강·음봉 파랑. **전략 N** 이평 + (옵션) **120·200** 장기 이평 오버레이. **(선택)** **누적 수익률**은 **별도 패널이 아닌**(v4.7) 가격 패널 **배경 twinx 음영** 또는 GUI **성과 텍스트**(최고·최저 누적 수익률 포함).
- **2패널(거래량):** `Volume` 막대, **당일 캔들 색(빨강/파랑)** 과 동일 규칙 — 끔(`show_chart_volume:false`) 시 **단일 패널** 렌더.
- **매매 타점:** 익봉 **시가** = 체결 가격과 동일하게 **해당 봉의 시가 세로 위치**에 **매수 ▲(`^`) / 매도 ▼(`v`)** 산점도.

**패널 세로 비율(가이드):** 가격 **6** : 거래량 **2** (`show_chart_volume` ON). 구현: `src/backtest_chart.py` 의 `make_backtest_figure` (구 **7:3:3 삼패널+v4.6**)는 **더 이상 렌더하지 않음**.

---

## 9. 파이썬 가상환경 (필수)

- **규칙:** `d:\develop\BackTesterKRX` 안에서 **`venv`** 를 만들고, **항상 활성화한 뒤** 개발·실행한다.
- **금지:** 전역(글로벌) 파이썬에 이 프로젝트용 패키지를 섞어 설치하지 않는다.
- **공통 환경:** 루트의 **`requirements.txt`** 만으로 같은 버전 묶음을 복제한다.

**최초 (PowerShell 예):**

```powershell
cd D:\develop\BackTesterKRX
python -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

**이후 매 세션:** `.\venv\Scripts\Activate.ps1` → `python main.py` (GUI 는 인자 없음, CLI 는 `--list` 등). **GUI 코드 저장 시 자동 재시작이 필요하면** `python main.py --watch` (`watchdog` 패키지 포함).

---

## 10. GUI 화면 (v2.1 ~ v2.6)

### 10.1 현재 상황 (팩트)

- **완료:** [1단계] 일봉 엔진, [3단계] 주봉·YAML/CLI, **§8** 캔들·거래량·이평·시가 타점·수익률 PNG(**`mplfinance`**, 한글 폰트 `rc` 적용), **v2.6** 좌측 슬림 입력·**실행 버튼 아래 5줄 성과**·**우측 전면 차트**. **v2.3** 에서 시험한 matplotlib **내장 줌/툴바**는 축 표시 오류 등으로 **v2.4에서 제거**, 우측은 **`output/backtest_report.png` 를 `CTkImage` 로 표시**한다.
- **미착수:** [4단계] 과거 시점 유니버스·생존 편향 최종 제거 — **GUI 안정화 후** 진행.

### 10.2 목적

- 검은색 터미널만이 아니라, **마우스로 조건 입력·결과 확인**하는 **데스크톱 GUI** 제공.

### 10.3 화면 구성

- **한 창**, 가로 비율 **1:2**(왼쪽·오른쪽 열 가중치 유저 조정 가능): **왼쪽 입력+요약**, **오른쪽 차트 전용**.
- **왼쪽:** 시장+키워드(한 줄), **종목 검색**, 리스트에서 **종목 1개 선택**, 일봉/주봉, 시작일+종료일(한 줄), 가상 원금, 이평 N+**120·200** 체크(한 줄), **백테스트 실행**, 그 아래 **성과 요약 5줄**(매매 횟수·최종 평가액·누적·연평균·MDD만, 소수 둘째 자리).
- **오른쪽:** 위에서 아래로 **(1) 「매매 규칙 · 전략 옵션」**(진입 필터·청산 보조 등 규칙 추가 시 이 영역 확장), **(2)** `output/backtest_report.png` 를 **`CTkImage`** 로 표시(추세 이평 **범례는 PNG 차트 좌측 상단**에 포함, GUI 별도 범례 없음 · v4.5), **(3)** 읽기 전용 매매 규칙 설명 텍스트. 창·패널 크기 변경 시 이미지는 **비율 유지**로 다시 맞춘다. (엔진 PNG 전체 구간, 툴바·축 줌 없음.)

### 10.4 기술
- **엔진:** `src/metrics.py` 의 `run_backtest_detailed()` 가 데이터 로드·시그널·시뮬·성과를 조립하고, 정적 PNG 차트는 **`src/backtest_chart.py`** 가 담당한다. `src/data_loader.py`, `strategy.py`, `simulator.py` 와 함께 시그널·체결·비용·성과 규칙을 수행한다. (GUI 위젯 비의존.)
- **GUI:** **CustomTkinter** — 화면 코드는 **`src/gui.py`**. 차트는 PNG 파일만 표시(필요 시 `PIL`·`CTkImage`). 실행은 루트 **`main.py`** 인자 없음. 의존성: `requirements.txt` 의 `customtkinter`, **`mplfinance`**, `matplotlib` 등.
- **CLI:** 루트 **`main.py`** 에 `--list` 등 인자가 있으면 argparse·표 출력으로 같은 엔진을 호출한다.
- **응답성:** 백테스트는 **백그라운드 스레드**, 완료 후 UI 갱신(창 멈춤 방지).

### 10.5 실행

```powershell
cd D:\develop\BackTesterKRX
.\venv\Scripts\Activate.ps1
python main.py
```

### 10.6 [중요] 소스코드 모듈화 고도화 지침

- **연관 문서:** 아래 지침과 맞물리는 내용(파일이 길어질 때 역할 단위 분리, 분리 후 README·작업지시서 등 폴더·역할 설명 갱신, UI·엔진 경계 유지)은 [문서화-원칙.md](문서화-원칙.md)의 **「코드가 길어질 때 (모듈화)」** 에도 적혀 있다.
- **금지:** 모든 코드를 `main.py` 한 파일에 몰아 넣지 않는다.
- **분리:** UI 창·레이아웃은 **`src/gui.py`**, YAML 반영·설정 dict 조립 등 GUI 헬퍼는 **`src/gui_helpers.py`** 에 둔다. 백테스트 핵심 계산은 **`src/data_loader.py`**, **`src/strategy.py`**, **`src/simulator.py`**, **`src/metrics.py`** 로 **파일 단위**로 나누고, 차트·공유 상수는 **`src/backtest_chart.py`**, **`src/backtest_constants.py`** 로 구분한다. (`metrics.py` 에 성과 지표·`run_backtest_detailed` 파이프라인 조립; PNG 렌더는 `backtest_chart.py`.)
- **UI 역할:** 사용자 입력(종목·기간·N·주기 등)을 **설정 dict** 로 엔진에 넘기고, 돌려받은 **텍스트 성과·이미지 경로**만 화면에 그린다.
- **향후 고도화:** `metrics.py` 안의 파이프라인 단계(예: 성과 전용 모듈)를 더 잘게 나누는 리팩터링은 필요 시 검토할 수 있다.

---

## 더 읽기

- [문서화-원칙.md](문서화-원칙.md)
- [백테스트-규칙.md](백테스트-규칙.md)
