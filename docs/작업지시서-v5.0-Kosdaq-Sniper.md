# 작업지시서: v5.0 코스닥 스나이퍼 (소액 필드 테스트)

> **상태:** 착수 (Phase A)  
> **브랜치:** `v5.0` (main·v4.0 병합 **금지** — v4.0 Phase H 동결 유지)  
> **원칙 문서:** `docs/money_management_principles.md`  
> **v4.0 참고:** Phase I 실험은 DoD 미달로 폐기됨 — v5는 SSOT·DoD·튜닝 루프를 분리 재개

---

## 0. 배경

| 구분 | v4.0 (동결) | v5.0 (목표) |
|------|-------------|-------------|
| 어장 | KOSPI 중심 Phase H 쌍바닥 | 코스닥 700억~5,000억 탄력 중소형 |
| 자본 | field_test 10만 / 슬롯 2 / 5만 고정 | 동일 (체결·슬리피지 검증) |
| 손익비 | SL -3% / TP +10% (H 동결) | **자본 규모 원칙** 동일 (-3% / +10%) |
| SSOT | `v4_0` · `phase_mode: h` | `v5_0` · Phase I 엔진 경로 |

v4 Phase I 단일 시나리오: PF 0.37 · -55.5% (10만) — **엔진 경로는 유지**, 파라미터·DoD·튜닝을 v5 전용으로 재검증한다.

---

## 진행 원칙

1. **Phase A → B → C** 순서 고정. DoD 미충족 시 다음 Phase 금지.
2. Phase 종료 시 `progress.md` §2 맨 위 요약 3~5줄.
3. 수정은 최소 diff. 러너는 `run_v5_*.py`, 설정은 `v5_0` YAML만.
4. `.env` 커밋 금지.

---

## Phase A — 인프라·베이스라인 (즉시 착수)

### A-1. 브랜치·SSOT

- [x] `git checkout -b v5.0` (v4.0 HEAD 기준)
- [x] `config/settings.yaml` → `v5_0` 블록
- [x] `src/v5_config.py` · `load_v5_config()` · `v5_to_v4_config()`
- [x] `run_v5_portfolio.py` → `outputs/v5_equity_curve.csv`, `v5_trades.csv`

### A-2. 베이스라인 실행

```bash
python run_v5_portfolio.py
```

### A-3. DoD (Phase A 완료)

- [ ] `python run_v5_portfolio.py` 정상 종료 (거래 0건이면 원인 로그·Pass 로그 기록)
- [ ] `outputs/v5_trades.csv` 생성 · Phase A 공통 검증(cash_after≥0) 통과
- [ ] `progress.md` §2에 v5.0 착수·베이스라인 수치 기록

**다음:** Phase B — `min_anchor_trade_krw` · `anchor_top_n` · `volume_dry_ratio` 그리드 (`run_v5_tune.py` 신설 예정)

---

## Phase B — 파라미터 그리드·DoD

| 지표 | 목표 (field_test 10만) |
|------|------------------------|
| Profit Factor | ≥ 1.0 |
| 최종 자산 | ≥ 100,000 원 (흑자) |
| 승률 | ≥ 45% (참고) |

- [ ] `run_v5_tune.py` (v4_tune 패턴, `v5_0` 오버라이드만)
- [ ] `outputs/v5_tune_results.csv` · `v5_tune_report.md`

---

## Phase C — 엔진 분리 (선택, DoD 충족 후)

- [ ] `portfolio_manager` Phase I 전용 모듈 분리 또는 `phase_mode` v5 전용 명칭
- [ ] `smart_money_cascade` Phase I 상수 → `v5_0.kosdaq_universe` 완전 이관

---

## 산출물 경로

| 파일 | 용도 |
|------|------|
| `outputs/v5_equity_curve.csv` | 일별 자산 |
| `outputs/v5_trades.csv` | 거래 상세 |
| `outputs/v5_tune_results.csv` | Phase B 그리드 |

---

**완료 일자:** Phase A 인프라 2026-06-03 · 베이스라인·DoD 미완
